services:
  portfolio:
    build: .
    container_name: dtmini-portfolio
    restart: unless-stopped
    ports:
      - "127.0.0.1:3100:3100"
    environment:
      - NODE_ENV=production
      - PORT=3100
      - HOSTNAME=0.0.0.0
      - CONTACT_EMAIL=${CONTACT_EMAIL}
      - CONTACT_FROM=${CONTACT_FROM}
      - RESEND_API_KEY=${RESEND_API_KEY}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3100/"]
      interval: 30s
      timeout: 5s
      retries: 3
    labels:
      - "traefik.enable=false"
